# Tasks

## Implementation

- [x] **Verify `cargo install thebakery` installs a working `bakery` binary** [packaging] M
  - Acceptance: `cargo publish --dry-run --locked` succeeds with zero warnings (fix `catagories` typo and the `license`/`license-file` duplication in `Cargo.toml`), `cargo install --path . --locked` from a clean checkout installs a working `bakery` binary whose `bakery --help` output is verified, the `exclude` list in `Cargo.toml` is audited so the published crate still builds (nothing required at build time from `scripts/`, `docker/`, `tests/`, `Makefile` or dotfiles is needed), it is confirmed whether version `1.1.14` already exists on crates.io and bumped via `make inc-version` if needed, a CI job is added (e.g. in `.github/workflows/buildntest.yml`) that runs `cargo publish --dry-run --locked` and `cargo install --path . --locked` on PRs, and `README.md` documents the `cargo install thebakery` install path for end users; renaming the crate/binary is explicitly out of scope.
  - Depends on: none
  - Modify: /mnt/workspace/mans/bakery/Cargo.toml, /mnt/workspace/mans/bakery/README.md, /mnt/workspace/mans/bakery/.github/workflows/buildntest.yml, /mnt/workspace/mans/bakery/.github/workflows/release.yml, /mnt/workspace/mans/bakery/Makefile
  - Create: none
  - Reuse: /mnt/workspace/mans/bakery/Makefile:cargo-install, /mnt/workspace/mans/bakery/Makefile:publish, /mnt/workspace/mans/bakery/src/cli/bakery.rs:env!("CARGO_PKG_VERSION")
  - Risks: `exclude` may drop files needed by a fresh `cargo install` (even though no `build.rs`/`include_str!` were found, `README.md` and `LICENSE` must remain present); `license` + `license-file` conflict may require picking one and updating packaging metadata; version `1.1.14` may already be on crates.io forcing a version bump via `scripts/do_inc_version.sh`; `cargo install` uses the host toolchain (glibc) while the project's default build target is `x86_64-unknown-linux-musl`, so dependencies assumed to be musl-static may behave differently; `cargo publish --dry-run` in CI cannot catch all crates.io server-side rejections.
- [x] **Rename binary, crate, and deb package to bkry (keep Bakery project name)** [packaging] L
  - Acceptance: the project is still referred to as "Bakery" in all prose (README, CLAUDE.md, .claude/CLAUDE.md, documentation/*) while the binary/CLI command is renamed to `bkry` everywhere users invoke it (all examples like `bakery build` become `bkry build`); `Cargo.toml` declares `name = "bkry"` and `[[bin]] name = "bkry"` with `path = "src/main.rs"` and `Cargo.lock` is regenerated via `cargo build --locked`; `BKRY_PKG_BUILD=test cargo test --locked` passes; `cargo publish --dry-run --locked` succeeds for the new `bkry` crate and the task notes document that crates.io cannot rename a published crate so `bkry` is a brand-new publication and `thebakery` should be yanked/deprecated with its README pointing at `bkry`; `cargo install --path . --locked` installs a working `bkry` binary and `bkry --help` prints usage with program name `bkry` (the clap `Command::new("bakery")` in `src/cli/bakery.rs` is changed to `"bkry"`); `scripts/do_deb_package.sh` produces a `.deb` whose control file has `Package: bkry`, `Provides: bakery`, `Replaces: bakery`, `Conflicts: bakery`, installs `/usr/bin/bkry`, and also creates a `/usr/bin/bakery -> bkry` symlink for backward compatibility; release artifact filenames become `bkry-x86_64-<variant>-v<version>.deb` with matching `bkry.deb` / `bkry-x86_64-<variant>.deb` symlinks, and `make install` still works end-to-end against the renamed artifact while preserving the `bakery` symlink; `scripts/do_build_release.sh`, `scripts/do_release.sh`, `scripts/do_install_bakery.sh`, `scripts/do_inc_version.sh`, `scripts/lib.sh`, `Makefile`, `.github/workflows/buildntest.yml`, and `.github/workflows/release.yml` are updated to reference `bkry` for binary, package, and release-asset names; `README.md`, `CLAUDE.md`, `.claude/CLAUDE.md`, and every file under `documentation/` (`build-bakery.md`, `build-config.md`, `docker.md`, `meta-layers.md`, `shell.md`, `starship.md`, `sub-commands.md`, `workspace-config.md`) are updated so prose uses "Bakery" for the project and `bkry` for the binary/command; tests that assert on the program name in help output or invoke the binary by name are updated and pass; the task explicitly documents that (a) the `BKRY_*` env var prefix already matches and is untouched, (b) the `ghcr.io/yanctab/bakery/bakery-workspace` Docker workspace image is out of scope, and (c) the GitHub repository name `yanctab/bakery` is out of scope.
  - Depends on: Verify `cargo install thebakery` installs a working `bakery` binary
  - Modify: /mnt/workspace/mans/claude/bakery/Cargo.toml, /mnt/workspace/mans/claude/bakery/Cargo.lock, /mnt/workspace/mans/claude/bakery/Makefile, /mnt/workspace/mans/claude/bakery/README.md, /mnt/workspace/mans/claude/bakery/CLAUDE.md, /mnt/workspace/mans/claude/bakery/.claude/CLAUDE.md, /mnt/workspace/mans/claude/bakery/src/main.rs, /mnt/workspace/mans/claude/bakery/src/cli/bakery.rs, /mnt/workspace/mans/claude/bakery/scripts/do_build_release.sh, /mnt/workspace/mans/claude/bakery/scripts/do_deb_package.sh, /mnt/workspace/mans/claude/bakery/scripts/do_install_bakery.sh, /mnt/workspace/mans/claude/bakery/scripts/do_release.sh, /mnt/workspace/mans/claude/bakery/scripts/do_inc_version.sh, /mnt/workspace/mans/claude/bakery/scripts/lib.sh, /mnt/workspace/mans/claude/bakery/.github/workflows/buildntest.yml, /mnt/workspace/mans/claude/bakery/.github/workflows/release.yml, /mnt/workspace/mans/claude/bakery/documentation/build-bakery.md, /mnt/workspace/mans/claude/bakery/documentation/build-config.md, /mnt/workspace/mans/claude/bakery/documentation/docker.md, /mnt/workspace/mans/claude/bakery/documentation/meta-layers.md, /mnt/workspace/mans/claude/bakery/documentation/shell.md, /mnt/workspace/mans/claude/bakery/documentation/starship.md, /mnt/workspace/mans/claude/bakery/documentation/sub-commands.md, /mnt/workspace/mans/claude/bakery/documentation/workspace-config.md
  - Create: none
  - Reuse: /mnt/workspace/mans/claude/bakery/Makefile:cargo-install, /mnt/workspace/mans/claude/bakery/Makefile:package, /mnt/workspace/mans/claude/bakery/Makefile:install, /mnt/workspace/mans/claude/bakery/scripts/lib.sh:get_bakery_version, /mnt/workspace/mans/claude/bakery/scripts/bkry.bashrc, /mnt/workspace/mans/claude/bakery/scripts/bkry-starship.toml, /mnt/workspace/mans/claude/bakery/src/cli/bakery.rs:Bakery::new
  - Risks: crates.io cannot rename an existing published crate so `bkry` is a brand-new publication and the `thebakery` name must be yanked/deprecated separately; `src/cli/bakery.rs` and the `Bakery` struct keep the old name in code while the user-facing binary is renamed, and reviewers may confuse "Bakery the project" with "bakery the legacy binary name" in docs — prose must consistently use capitalised "Bakery" for the project and code-formatted `bkry` for the CLI; `do_deb_package.sh` currently writes `Package: bakery`, installs `/usr/bin/bakery`, and names artifacts `bakery-x86_64-*.deb`, so switching to `bkry` must include `Replaces:`/`Conflicts:`/`Provides: bakery` to keep apt/dpkg upgrade paths working and must create the `/usr/bin/bakery -> bkry` symlink inside the staging dir before `dpkg-deb --build` so `dpkg` ownership checks pass; `make install` uses `sudo dpkg -i artifacts/bakery.deb` and must be updated to the new artifact name while the backward-compat symlink `artifacts/bakery.deb -> bkry.deb` (or an updated target) keeps existing muscle memory working; clap program-name changes will break any tests asserting on `bakery` in stdout/stderr; the `bakery-workspace` GHCR image, the `yanctab/bakery` repo name, and the `BKRY_*` env var prefix are intentionally untouched and must not be swept up by a global find/replace; documentation updates are wide-reaching and easy to miss — a final `rg -i '\bbakery\b' -- '*.md'` audit is needed to ensure no user-facing command example still says `bakery <subcommand>`.
